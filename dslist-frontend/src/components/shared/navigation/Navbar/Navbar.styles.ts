import styled from "styled-components"

export const Header = styled.header`
display: flex;
align-items: center;

height: 80px;
width: 100%;
padding: 0px 40px;

background-color: var(--blue-900);

    > a {
        color: var(--gray-100);
    }

`